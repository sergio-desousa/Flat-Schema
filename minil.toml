name = "Flat-Schema"
authority = "cpan:SDS"
license = "perl_5"

# README.md is ALWAYS regenerated from POD SSOT.
readme_from = "lib/Flat/Schema.pm"

tag_format = "v%v"

[release]
pause_config = 1

[Metadata]
x_static_install = 1
